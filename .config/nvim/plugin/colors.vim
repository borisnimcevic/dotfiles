" I have no idea why, this beats me. I have to turn off the syntax
" to not have lag when typing, even though the syntax is still on when
" I open the files...
syntax off
" colorscheme codedark
" let g:airline_theme = 'codedark'
" let g:rainbow_active = 1
colorscheme gruvbox
let g:gruvbox_contrast_dark = 'medium'
set background=dark
" let g:gruvbox_contrast_light = 'hard'
"set background=light
